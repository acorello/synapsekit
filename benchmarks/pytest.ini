[pytest]
addopts =
  --benchmark-autosave
  --benchmark-histogram
  --benchmark-sort=mean
  --benchmark-json=benchmarks/benchmark.json
  --benchmark-warmup=on
  --benchmark-warmup-iterations=1
  --benchmark-min-rounds=5
